{
  "name": "tether-foundation-browser-proof",
  "version": "0.0.0-prototype",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy --keep-vars && wrangler d1 migrations apply DB --remote && wrangler deploy --keep-vars"
  },
  "dependencies": {
    "@cloudflare/containers": "0.3.7",
    "@cloudflare/puppeteer": "1.1.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20260627.1",
    "typescript": "6.0.3",
    "wrangler": "4.105.0"
  }
}
